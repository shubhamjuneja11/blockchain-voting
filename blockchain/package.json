{
  "name": "llblockchain",
  "version": "0.1.0",
  "description": "a javascript blockchain",
  "main": "index.js",
  "scripts": {
    "start": "mongod --dbpath ./data --port 3001 & nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "LiveLike",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^5.0.0",
    "mongodb": "^3.0.2"
  },
  "devDependencies": {
    "nodemon": "^1.14.12"
  }
}
